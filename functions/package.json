{
  "name": "functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "tsc || echo no-ts",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint --ext .js,.ts ."
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.4.0"
  }
}
